{
  "finding_id": "b4a0c783-c52e-4860-bfbd-a24961accb50",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T21:52:34.020918+00:00",
  "finding": "Tailscale node sharing: When a user from another tailnet is \"Shared in\" (via invite link), their node appears in your network map but cross-user ACL enforcement is stricter than same-tailnet nodes. The `*` \u2192 `*:*` ACL in the `acls` section should allow all traffic but the local daemon's ts-input chain doesn't reflect this for shared nodes.",
  "impact": 0.8,
  "goal_id": null,
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "Tailscale node sharing: When a user from another tailnet is \"Shared in\" (via invite link), their node appears in your network map but cross-user ACL enforcement is stricter than same-tailnet nodes. The `*` \u2192 `*:*` ACL in the `acls` section should allow all traffic but the local daemon's ts-input chain doesn't reflect this for shared nodes.",
    "impact": 0.8
  }
}
