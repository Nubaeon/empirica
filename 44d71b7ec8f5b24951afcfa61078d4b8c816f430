{
  "finding_id": "afa33513-3feb-4cfa-99ae-c0a4be2f73f5",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T21:52:30.426606+00:00",
  "finding": "Tailscale cross-user SSH: The ts-input iptables chain managed by tailscaled contains a blanket DROP rule for 100.64.0.0/10 that blocks ALL inbound peer traffic. For shared/cross-tailnet nodes, Tailscale does NOT auto-generate per-peer ACCEPT rules from ACLs. Fix: manually insert iptables rule BEFORE the DROP: `sudo iptables -I ts-input 3 -s <peer-tailscale-ip> -j ACCEPT`. This rule is ephemeral - wiped on tailscaled restart.",
  "impact": 0.9,
  "goal_id": null,
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "Tailscale cross-user SSH: The ts-input iptables chain managed by tailscaled contains a blanket DROP rule for 100.64.0.0/10 that blocks ALL inbound peer traffic. For shared/cross-tailnet nodes, Tailscale does NOT auto-generate per-peer ACCEPT rules from ACLs. Fix: manually insert iptables rule BEFORE the DROP: `sudo iptables -I ts-input 3 -s <peer-tailscale-ip> -j ACCEPT`. This rule is ephemeral - wiped on tailscaled restart.",
    "impact": 0.9
  }
}
