{
  "finding_id": "5809397a-1cb9-4e7c-b2b2-782e2cd04f2a",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T21:53:06.125746+00:00",
  "finding": "Tailscale cross-user SSH working recipe: (1) Invite external user via Tailscale admin share link, (2) They accept and their node appears in your network, (3) Add UFW rule: `sudo ufw allow from 100.64.0.0/10 to any port 22`, (4) Insert iptables rule BEFORE ts-input DROP: `sudo iptables -I ts-input 3 -s <peer-ip> -j ACCEPT`, (5) They connect with regular `ssh user@tailscale-ip` (NOT `tailscale ssh`). The iptables rule is ephemeral and needs re-adding after tailscaled restart.",
  "impact": 0.95,
  "goal_id": null,
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "Tailscale cross-user SSH working recipe: (1) Invite external user via Tailscale admin share link, (2) They accept and their node appears in your network, (3) Add UFW rule: `sudo ufw allow from 100.64.0.0/10 to any port 22`, (4) Insert iptables rule BEFORE ts-input DROP: `sudo iptables -I ts-input 3 -s <peer-ip> -j ACCEPT`, (5) They connect with regular `ssh user@tailscale-ip` (NOT `tailscale ssh`). The iptables rule is ephemeral and needs re-adding after tailscaled restart.",
    "impact": 0.95
  }
}
