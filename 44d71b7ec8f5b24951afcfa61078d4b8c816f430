{
  "finding_id": "37207ac4-5b47-4fc0-8d55-a18122089182",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T22:48:51.935841+00:00",
  "finding": "ROOT CAUSE FOUND: Tailscale daemon-level packet filter (visible in `tailscale debug netmap` \u2192 PacketFilter) explicitly excludes shared/cross-tailnet node IPs via surgical CIDR carving. For Philipp (100.79.215.20), 114 CIDR ranges cover the entire 100.64.0.0/10 space EXCEPT his exact IP. This filtering happens INSIDE the daemon before packets reach tailscale0. No iptables/UFW/ACL change can fix this. The `*` wildcard in ACLs does NOT include cross-tailnet shared nodes at the packet filter level. This is by design in Tailscale's security model.",
  "impact": 1.0,
  "goal_id": "ea9a80c4-ba88-47ba-8787-cc0dbc1a586c",
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "ROOT CAUSE FOUND: Tailscale daemon-level packet filter (visible in `tailscale debug netmap` \u2192 PacketFilter) explicitly excludes shared/cross-tailnet node IPs via surgical CIDR carving. For Philipp (100.79.215.20), 114 CIDR ranges cover the entire 100.64.0.0/10 space EXCEPT his exact IP. This filtering happens INSIDE the daemon before packets reach tailscale0. No iptables/UFW/ACL change can fix this. The `*` wildcard in ACLs does NOT include cross-tailnet shared nodes at the packet filter level. This is by design in Tailscale's security model.",
    "impact": 1.0
  }
}
