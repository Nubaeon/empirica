{
  "finding_id": "852ab32c-88b8-43b1-9bde-03b201ebd93d",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T21:52:32.093542+00:00",
  "finding": "Tailscale SSH ACL rules: The `dst` field in SSH rules only accepts `autogroup:self` or `tag:*` targets. You CANNOT reference other users (e.g., `p.schwinger@gmail.com`) in dst. Error: \"users in dst are only allowed from the same user\". This is a fundamental limitation of Tailscale SSH ACL, not a misconfiguration.",
  "impact": 0.85,
  "goal_id": null,
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "Tailscale SSH ACL rules: The `dst` field in SSH rules only accepts `autogroup:self` or `tag:*` targets. You CANNOT reference other users (e.g., `p.schwinger@gmail.com`) in dst. Error: \"users in dst are only allowed from the same user\". This is a fundamental limitation of Tailscale SSH ACL, not a misconfiguration.",
    "impact": 0.85
  }
}
