{
  "finding_id": "acd2dbaa-b270-47b0-a213-d7615d14e4dd",
  "project_id": "748a81a2-ac14-45b8-a185-994997b76828",
  "session_id": "a9063529-1a40-4b53-95e0-7d8c6c9a03d2",
  "ai_id": "claude-code",
  "created_at": "2026-01-29T22:35:57.323016+00:00",
  "finding": "Tailscale SSH intercept trap: When `tailscale set --ssh=true` is enabled, Tailscale intercepts ALL SSH connections on the Tailscale interface BEFORE they reach the system sshd. If the Tailscale SSH ACL rejects the connection (e.g., cross-user dst), it silently drops - no error, just timeout. Fix: disable Tailscale SSH (`tailscale set --ssh=false`) to let connections pass through to regular sshd. CRITICAL: always disable Tailscale SSH when using regular SSH for cross-user connections.",
  "impact": 0.95,
  "goal_id": "ea9a80c4-ba88-47ba-8787-cc0dbc1a586c",
  "subtask_id": null,
  "subject": null,
  "finding_data": {
    "finding": "Tailscale SSH intercept trap: When `tailscale set --ssh=true` is enabled, Tailscale intercepts ALL SSH connections on the Tailscale interface BEFORE they reach the system sshd. If the Tailscale SSH ACL rejects the connection (e.g., cross-user dst), it silently drops - no error, just timeout. Fix: disable Tailscale SSH (`tailscale set --ssh=false`) to let connections pass through to regular sshd. CRITICAL: always disable Tailscale SSH when using regular SSH for cross-user connections.",
    "impact": 0.95
  }
}
